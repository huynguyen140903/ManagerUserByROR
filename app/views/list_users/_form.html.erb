<%= form_with(model: list_user) do |form| %>
  <% if list_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(list_user.errors.count, "error") %> prohibited this list_user from being saved:</h2>

      <ul>
        <% list_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
