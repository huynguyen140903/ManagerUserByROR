<%= form_with(model: confirm) do |form| %>
  <% if confirm.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(confirm.errors.count, "error") %> prohibited this confirm from being saved:</h2>

      <ul>
        <% confirm.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
