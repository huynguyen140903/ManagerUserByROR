<%= form_with(model: add_user) do |form| %>
  <% if add_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(add_user.errors.count, "error") %> prohibited this add_user from being saved:</h2>

      <ul>
        <% add_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
