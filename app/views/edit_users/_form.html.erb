<%= form_with(model: edit_user) do |form| %>
  <% if edit_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(edit_user.errors.count, "error") %> prohibited this edit_user from being saved:</h2>

      <ul>
        <% edit_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
