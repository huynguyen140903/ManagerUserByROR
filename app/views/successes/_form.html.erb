<%= form_with(model: success) do |form| %>
  <% if success.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(success.errors.count, "error") %> prohibited this success from being saved:</h2>

      <ul>
        <% success.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
